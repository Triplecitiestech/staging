# Triple Cities Tech — CLAUDE.md
*Last updated: 2026-08-28 · Owner: Kurtis (kurtis@triplecitiestech.com)*

> **The operating manual for every AI session in this repo.** Read this file first, then the bootstrap docs below. This file holds the rules you must never break plus an index to everything else; deep detail lives in `/docs`. If a section here grows, push the detail into `/docs` and link it — keep this file lean so nothing gets skimmed. When you touch this file, bump the date above.

---

## A · What this repo is

Production codebase for Triple Cities Tech, an MSP (managed IT services provider). One Next.js 15 app on Vercel serving: the public marketing site, the staff admin portal (projects, tickets, reporting, SOC, marketing, CFO), the customer portal, and AI agents (blog generation, SOC triage, report assistant). Integrates Autotask PSA, Microsoft 365/Graph, Datto RMM/EDR, RocketCyber, DNSFilter, SaaS Alerts, QuickBooks, and Sequence. **Stage: shipped — live production at https://www.triplecitiestech.com with active daily development.**

## B · The Goal

- **Why it exists:** one platform for TCT's entire MSP operation — replaces scattered vendor portals with a single pane for staff and a self-service portal for customers.
- **Done looks like:** every subsystem works end-to-end in production, on desktop AND mobile, with self-healing pipelines and no manual babysitting.
- **Out of scope:** features that duplicate an existing subsystem (extend, don't rebuild), and removing the Temporary Development Shortcuts (see `docs/gotchas.md`) without explicit owner approval.

## Session Bootstrap — read in order

1. `docs/architecture.md` — system architecture, data flows, integration diagrams
2. `docs/system-map.md` — codebase map: which files own which subsystem
3. `docs/data-model.md` — database schema, entity relationships
4. `docs/session-summary.md` — current state, recent changes, key decisions
5. `docs/current-tasks.md` — active development work
6. `docs/gotchas.md` — **the full gotcha list + subsystem field notes (Autotask, M365, SOC, SaaS Alerts, portal, CFO). Mandatory — the digest below is not a substitute.**

Implementation/testing rules: `docs/coding-standards.md`, `docs/qa-standards.md`, `docs/UI_STANDARDS.md`.
Docs under `/docs/plans/`, `/docs/reference/`, `/docs/archive/` are supporting material — they do NOT override architecture decisions unless explicitly referenced.

---

## Core Rules

1. **Be autonomous.** Diagnose and fix errors yourself (build failures, lint, broken imports). Escalate only for things you genuinely cannot obtain: credentials, business decisions, ambiguous product requirements.
2. **Every change is tested before deploy.** `npm run build`, `npm run lint`, and `npm run test:e2e` must all pass. Review your own `git diff`. Check UI at mobile (`sm`/`md`) AND desktop (`lg`+) breakpoints. Completion means it works end-to-end, not that it compiles. Full QA process: `docs/coding-standards.md` + `docs/qa-standards.md`.
3. **Every completed task is committed and pushed.** Commit per logical unit; push immediately with `git push -u origin <branch>`; retry on network failure with backoff (2s, 4s, 8s, 16s). Vercel auto-deploys every branch (preview) and `main` (production).
4. **After deployment, verify the live result** — or tell the user exactly what to verify and on which URL, calling out mobile-affecting changes.
5. **When something breaks, fix it — don't report it and stop.** Loop until done or genuinely blocked on user input.
6. **Keep the memory layer up to date.** New lessons go to `docs/gotchas.md`; add a one-liner to the Critical Gotchas digest below only if missing it would break production. Bump this file's date on every edit.
7. **Hand-offs to the user must be explicit and assume nothing.** State WHAT to do, in WHAT ORDER, in WHICH SYSTEM — numbered steps, exact URLs, page/button/field names, example payloads. Vague hand-offs are not acceptable.

## Reuse Before You Build

- **Search the repo first** — 100+ API routes, 20+ reporting modules, 30+ component dirs; the feature may already exist. Extend or refactor existing modules; replacement requires explicit user approval.
- **No parallel implementations** — exactly one Autotask client, one Prisma client, one permission system, one ticket adapter. Never create `*-v2`, `*-new`, `*-alt`, or sibling utility files.
- **Prefer modifying existing files.** Create a new file only when separation is architecturally justified — and say why.
- **Before coding a non-trivial change**: read the relevant source-of-truth modules, explain how the work fits the existing architecture, and confirm whether the feature already exists in part.

## Source of Truth Modules

| Subsystem | Authoritative Module | Notes |
|-----------|---------------------|-------|
| Autotask API client | `src/lib/autotask.ts` | All Autotask REST calls |
| Prisma / Database | `src/lib/prisma.ts` | Singleton PrismaClient with PrismaPg adapter |
| Raw pg pool | `src/lib/db-pool.ts` | `getPool()` — HR/M365/reporting raw-SQL routes |
| Security utilities | `src/lib/security.ts` | `checkRateLimit()`, `checkCsrf()`, input sanitization |
| Resilience utilities | `src/lib/resilience.ts` | `withRetry()`, `withTimeout()`, `withCircuitBreaker()`, `withDbRetry()`, `classifyError()`, `structuredLog` — no ad-hoc retry loops |
| Cron job wrapper | `src/lib/cron-wrapper.ts` | Standardized cron execution: auth, retry, timeout |
| Staff permissions | `src/lib/permissions.ts` | SUPER_ADMIN, ADMIN, BILLING_ADMIN, TECHNICIAN |
| Unified ticket system | `src/lib/tickets/` | Adapters/types consumed by all ticket views |
| SOC engine | `src/lib/soc/` | Engine, correlation, rules, prompts, enrichment |
| Reporting engine | `src/lib/reporting/` | 20+ modules: sync, aggregation, analytics, SLA |
| Autotask contact sync | `src/lib/autotask-contact-sync.ts` | `syncAutotaskContacts({ companyId? })` — single source for the AT→DB contact pull |
| Blog generation | `src/lib/blog-generator.ts` | AI content generation via Claude API |
| Demo mode | `src/lib/demo-mode.ts` | Contoso Industries demo data, in-memory only |
| Error logging | `src/lib/error-logger.ts` | Centralized logging to ErrorLog model |
| API usage tracking | `src/lib/api-usage-tracker.ts` | AI/email/API usage → ApiUsageLog |
| Customer portal data / auth | `src/lib/onboarding-data.ts` / `src/lib/onboarding-session.ts` | Portal loading + sessions |
| M365 / Graph credentials | `src/lib/graph.ts` | `getTenantCredentials*()` — ONLY place that knows legacy vs multi-tenant mode |
| API route auth (secrets) | `src/lib/api-auth.ts` | `checkSecretAuth()` — header + query param auth |
| API response envelope | `src/lib/api-response.ts` | `apiSuccess()`, `apiError()` |
| Env validation | `src/lib/env-validation.ts` | Startup validation of env vars |
| SaaS Alerts client | `src/lib/saas-alerts.ts` | External Partner API only (portal API is Cloudflare-blocked) |
| CFO dashboard | `src/lib/cfo/` | Sequence + QuickBooks; `store.ts` is raw-pg `cfo_settings`, NOT Prisma |
| Connector usage telemetry | `src/lib/connector/telemetry.ts` | One row per MCP tool call in raw-pg `connector_tool_calls`; hooked ONCE via `recordingServer()`. Records names/classifications/outcomes/timings/response SIZE only — never arguments, response bodies or error messages |
| Kaseya Quote Manager | `src/lib/kaseya-quote-manager.ts` | Read-only Datto Commerce client. `get()` is the ONLY network path (GET by construction); the vendor API has no write surface — all 39 spec operations are GET. Paging 1-INDEXED, no total count in any response. MCP tools: `src/lib/mcp-kaseya-quote-manager-tools.ts` (generated from `KQM_RESOURCES`, coverage asserted against the captured spec by test) |
| Sales calculator | `src/lib/sales-calculator/` + `src/config/sales-calculator/` | Internal quoting tool at `/admin/sales-calculator`. Money = `pricing.json` defaults + DB overrides (`sales_calc_pricing_overrides`, edited at `/admin/sales-calculator/pricing`) — never edit the calc code for pricing. Saved quotes = raw-pg `sales_calc_saved_quotes` (inputs only; reprices on load). Effective pricing is read server-side ONLY via `src/lib/sales-calculator/pricing-source.ts`; read-only MCP exposure = `src/lib/mcp-sales-pricing-tools.ts` |

**Rule**: need that functionality? Import and extend the existing module. Never create a duplicate client or service layer.

## C · Stack & Commands

Next.js 15 (App Router) · React 18 · TypeScript strict · Tailwind CSS 3.4 · PostgreSQL (Vercel Postgres) + Prisma ORM 7.2 · NextAuth.js 5 (Azure AD OAuth) · Resend · Anthropic Claude API · Autotask PSA REST · Cloudflare Turnstile · Recharts · Vercel (region `iad1`).

```bash
npm run dev             # Dev server
npm run build           # Prisma migrate deploy + next build (MUST PASS)
npm run lint            # ESLint (MUST PASS)
npm run test:e2e        # Playwright e2e — covers ALL systems, run before every push
npm run test:e2e:ui     # Playwright interactive UI
npm run debug:failures  # Review e2e failure summaries
npm run seed            # Seed database
# Remote e2e vs deployed preview: BROWSERBASE_API_KEY=… BROWSERBASE_PROJECT_ID=… PLAYWRIGHT_BASE_URL=https://preview.vercel.app npm run test:e2e
```

**Layout**: `src/app/` (pages + 100+ API routes: `(marketing)`, `admin/`, `api/`, `blog/`, `onboarding/`), `src/components/` (per-feature dirs + `ui/`), `src/lib/` (subsystem modules — table above), `prisma/schema.prisma` (1300+ lines, 35+ models), `tests/e2e/` (30+ Playwright specs). Full map: `docs/system-map.md`.

**Key features** (one-liners; details in `docs/architecture.md` + `docs/gotchas.md`): AI blog pipeline (generate → approve → scheduled publish via crons) · Project management (Company → Project → Phase → PhaseTask) · Autotask PSA sync · SOC Analyst Agent (`/admin/soc`, cross-stack AI triage) · Reporting & analytics (`/admin/reporting`, self-healing pipeline) · Unified ticket system · Marketing campaigns (`/admin/marketing`) · Customer portal (`/onboarding/[companyName]`) · CFO dashboard (`/admin/cfo`) · Platform monitoring · Demo mode.

## Code Conventions

- **TypeScript**: strict, no `any`. Interfaces for all props/params. `.ts` utils, `.tsx` components.
- **React/Next**: server components by default; `'use client'` only when needed. PascalCase components, camelCase utilities. Path alias `@/*` → `./src/*`.
- **Styling**: Tailwind, mobile-first (`base` → `md:` → `lg:`). Standard grids: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`. Every layout change checked at all breakpoints.
- **FORBIDDEN COLORS**: never `yellow-*`, `amber-*`, `brown-*`, gold/mustard — and avoid `orange-*` (renders amber on our dark backgrounds). Use `violet`, `rose`, `red`, `cyan`, `blue`, `green`, `emerald`. Applies to backgrounds, text, borders, gradients. See `docs/UI_STANDARDS.md`.
- **API routes**: try/catch with `NextResponse.json`; validate input; structured responses; catch blocks return 4xx/5xx with `{ error }` — never 200 with empty data.

## Database — non-negotiables

- **`prisma migrate deploy` does NOT actually run on this DB.** Production has no `_prisma_migrations` table; every column was created by raw `ALTER TABLE IF NOT EXISTS` in `src/app/api/migrations/run/route.ts`. **New schema field = schema.prisma change + ALTER TABLE in that route + POST `/api/migrations/run` after deploy.** A migration file alone is a no-op. Full detail: `docs/gotchas.md`.
- **Never run `prisma migrate` directly on production** — use the API migration endpoints.
- **Raw-pg (NOT Prisma) tables**: reporting (`report_*`), SOC (`soc_*`), CFO (`cfo_settings`). HR and M365 routes also use raw pg via `getPool()` — do not convert them to Prisma.
- **Both pool files (`prisma.ts`, `db-pool.ts`) MUST keep SSL config and `globalThis` caching** — removing either breaks all of production.
- No hard deletes (status fields / `deletedAt`). Raw SQL uses quoted camelCase columns (`"companyId"`) except HR/M365 snake_case `@map()` columns. JSONB inserts need `$1::jsonb`.
- **Expand-contract**: schema changes shipped with code must be additive (ADD COLUMN / CREATE TABLE); never DROP or RENAME in the same deploy as the code change. The migration must be applied **before** the code that needs it goes live.
- **Planned fix**: `docs/runbooks/MIGRATIONS_RETROFIT.md` (operator-run) baselines `_prisma_migrations` so `prisma migrate deploy` works and the manual POST step disappears. Until executed, the raw-SQL route remains the source of truth.

## Ship Cycle & Git

```
Plan → Implement → Verify (build + lint + e2e) → Review (git diff) → Commit → Push → Confirm to user
```
- Branches: `claude/[description]-[sessionId]`. **Never push to `main`.** Auto-merge to main is **gated**: every push to a `claude/**` branch must pass secret-scan + lint + schema-drift + build + unit tests before `.github/workflows/auto-merge-claude.yml` merges it. A failed gate = no merge = no production deploy. The full e2e suite still runs against the Vercel preview but is **non-blocking** (owner decision 2026-07-10, to cut ~30-40 min of merge latency): a red `e2e-preview` job on a merged commit means production is already live with something the suite flagged — investigate immediately, never ignore it.
- `[skip-e2e]` in a commit message skips the (now informational) e2e run — e.g. Vercel integration down, no preview will appear; record any use in `docs/session-summary.md`.
- Commit style: imperative summary, optional bullets. Commit + push after every logical unit — don't batch.
- If verify fails, go back to implement. Never commit a broken build. Local verify still matters: the gates catch what you miss, but a red gate is a wasted deploy iteration.
- Use subagents for broad codebase searches, parallel independent tasks, and keeping context clean during large refactors.

## D · Decisions

*One line each: date · what · why. Long-form reasoning lives in `docs/session-summary.md` and `docs/archive/`.*

- `2026-03-20` — Removed customer portal password gate; the shared URL is the access control.
- `2026-03-22` — Deleted legacy HrRequestWizard; schema-driven FormRenderer is the only portal form engine.
- `2026-03-27` — Health-monitor job names standardized to underscores after a hyphen mismatch caused false alerts every 15 min.
- `2026-04-26` — Confirmed Prisma's migration runner has never applied to prod; declared `/api/migrations/run` the migration source of truth until `_prisma_migrations` is retrofitted.
- HR routes moved to raw pg (`getPool()`) because Prisma caused production 500s.
- New M365 onboardings default to `multi_tenant` admin-consent mode; legacy per-tenant app regs remain supported (`m365_consent_mode`).
- TECHNICIAN role granted `invite_customers` + `manage_customer_roles` (session 8) so techs can run the onboarding wizard.
- SOC made cross-stack (RocketCyber + Datto RMM/EDR + DNSFilter + SaaS Alerts) with hard per-customer scoping after a cross-tenant data leak.
- SaaS Alerts server calls moved to the External Partner API; the portal API is Cloudflare-blocked for server-to-server.
- `2026-06-09` — Restructured CLAUDE.md to the operating-manual format; full gotcha list moved to `docs/gotchas.md` (mandatory bootstrap read #6).
- `2026-06-09` — Gated the auto-merge: claude/** pushes now require secret-scan + lint + schema-drift + build + unit tests + e2e-vs-preview before merging to main. Schema-drift check wired into Vercel builds. `_prisma_migrations` retrofit planned (`docs/runbooks/MIGRATIONS_RETROFIT.md`).
- `2026-06-16` — Added a live, multi-year **TBR / Customer History export**: `GET /api/reports/tbr-export` (read-only; auth = staff session OR `MIGRATION_SECRET`) pulls a company's full ticket history straight from Autotask — bypassing the reporting sync's ~30-day DB cache — plus Datto RMM, with a `/admin/reporting/customer-history` admin UI (live Autotask customer typeahead). Ticket counts are split **human-support vs proactive-monitoring** by queue. Full reference: `docs/reference/TBR_DATA_CAPABILITIES.md`.
- `2026-06-16` — Autotask `nextPageUrl` GET 405s on `includeFields` queries and filters `createDate` at day granularity; large pulls now paginate by recursively splitting the createDate window (`queryOnePage`) and de-dupe by ticket id. Lesson: raw Autotask "ticket" counts include automated monitoring/alert queues — split human vs monitoring for customer-facing reports. See `docs/gotchas.md` → Autotask.
- `2026-06-26` — SOC assessment hardened with a deterministic guardrail layer over the LLM verdict (`applyGuardrails` in `src/lib/soc/engine.ts`): historical recurrence reduces novelty only (never raises benign confidence or lowers risk; it's the agent's own prior closes, not corroboration), IP reputation / geolocation / timing / corroboration are evaluated as separate axes, confidence is capped when uncorroborated (`confidence_uncorroborated_cap`), and IAM/MFA changes default to "confirm with the user before closing". Full detail: `docs/gotchas.md` → SOC.
- `2026-06-27` — SOC identity/MFA alerts now confirm against the customer's OWN M365 tenant (`src/lib/soc/m365-identity.ts`, reusing `getTenantCredentials` + `graphRequest`): Entra audit log + sign-ins + remaining auth methods; a tenant-confirmed benign re-enrollment lifts the confidence cap and counts as positive benign evidence. Also fixed SaaS Alerts correlation to match `customerId` OR `organizationId` (the bare `customerId` term returned 0 events for SaaS-sourced tickets). Autotask auto-posting unchanged. Full detail: `docs/gotchas.md` → SOC.
- `2026-06-29` — Added a reusable **WAN Reliability (ISP/SLA) report** by extending the existing Domotz client (`src/lib/domotz.ts` — added paginated `getAllAgents`, agent/device detail, and uptime/event/RTD/speed history; no new client). New module `src/lib/reporting/wan-reliability/` (pure analyzer + service + JSON/Markdown/text/HTML formatters), `GET /api/reports/wan-reliability` (staff session OR `MIGRATION_SECRET`) + `…/sites` picker, and `/admin/reporting/wan-reliability` UI. Outage timeline from Domotz `downtime_intervals` (collector connectivity = truest WAN signal; device reachability when a device is chosen). Domotz history endpoints default to a 1-week window, so 90-day pulls are time-chunked. Full detail: `docs/reference/WAN_RELIABILITY_REPORT.md` + `docs/gotchas.md` → Domotz.
- `2026-06-30` — **Reframed that report to "Site Connectivity & Stability"** after a false-negative was confirmed: at a WAN-failover site (Meraki MX + Starlink/LTE) the firewall fails over so Domotz reachability shows 100% even when the primary circuit dropped (Montrose: 10 primary drops incl 17-min, reported 100%). We have no Meraki API, so the report now headlines collector connectivity (whole-site dark) — NOT ISP uptime — shows an SLA verdict ONLY for admin-confirmed single-WAN sites, and otherwise shows a prominent failover-masking caveat (never "SLA-compliant"/"no escalation" from reachability). Added **Phase 2 failover detection**: ingest webhook-only `agent_wan_change` events via `POST /api/webhooks/domotz` (token `DOMOTZ_WEBHOOK_TOKEN`) into the shared `compliance_webhook_events` sink (`source='domotz'`), surfaced as a first-class "N failover events" metric. Failover-capability is auto-detected from gateway model + a per-site override (raw-pg `domotz_site_settings`). Also: data-coverage measured empirically (no 100%-over-no-data), cadence-artifact flagging, and failover events paired into **episodes** (`pairFailoverEpisodes`) for an *estimated* primary-circuit downtime. Full detail: `docs/reference/WAN_RELIABILITY_REPORT.md` + `docs/gotchas.md` → Domotz.

- `2026-07-03` — MCP connector gained **Autotask config visibility** (11 live read tools + `autotask_config_query`/`autotask_entity_capabilities`) and a **structurally gated config-write tier**: stage (never writes) → human approval at `/admin/connector/staged-writes` (staff auth the MCP token can't reach) → drift-checked single-use execute; kill switch `CONNECTOR_CONFIG_WRITES_ENABLED`. Verified REST boundaries recorded in `docs/gotchas.md` → Autotask: no workflow rules / notification templates / widgets / SLA definitions / status→SLA-event mapping in the API — the status mapping is served from an owner-maintained overlay, provenance-labelled, never as API data.
- `2026-07-04` — MCP connector gained **per-site UniFi tools** via the Cloud Connector Proxy (new typed client `src/lib/ubiquiti-proxy.ts`; `ubiquiti.ts` untouched): `unifi_resolve_site` (never guesses), ~18 per-site reads (secret-redacted, typed errors incl. `FIRMWARE_UNSUPPORTED`/`CONSOLE_OFFLINE`), tier-1 attributed actions (restart device, PoE power-cycle, guest auth, vouchers) and tier-2 staged config writes (firewall/zones/networks/WLAN-edit/ACL/DNS/adopt) through the SAME human-approval gate — all single console/site/target by schema, kill switch `CONNECTOR_UNIFI_WRITES_ENABLED`. Tool surface = official Integration API only (OpenAPI 10.1.84 verified); port forwards/routes/events/health/locate/block are NOT in the official API and are omitted-with-reason. Fleet firmware remediation: `unifi_probe_consoles` / `scripts/probe-unifi-consoles.ts`. Reference: `docs/unifi-site-tools.md`; field notes: `docs/gotchas.md` → UniFi.
- `2026-07-05` — Offboarding ticket record is now **reconciliation-based**: every action requested on the form appears exactly once in PROVISIONING RESULTS as `[DONE]/[FAILED]/[MANUAL]/[QUEUED]/[NOT RUN]` (`src/lib/hr/offboarding-actions.ts`), after ticket T20260704.0004 silently dropped a requested shared-mailbox conversion (no Graph API for mailbox conversion + logging gated on an array-only answer shape). New offboarding form fields MUST be added to `deriveRequestedOffboardingActions()`, not just the pipeline. IT Glue SOPs 16573760/14639952/20377379 rewritten portal-first. Full notes: `docs/gotchas.md` → HR Onboarding/Offboarding Automation.
- `2026-07-05` — **Mailbox conversion + delegate access automated** for enabled tenants via an **Azure Automation Exchange runner** (EXO PowerShell v3 app-only cert auth; dedicated multi-tenant app "TCT Exchange Automation"; per-tenant custom role group scoped to `Mail Recipients`, NOT Exchange Administrator — "RBAC for Applications" rejected: Graph/EWS only, no PowerShell). Platform: `src/lib/exchange-online.ts` (HMAC webhook dispatch; raw-pg `hr_exchange_jobs`/`exo_tenant_config`), `/api/hr/exchange-callback` (asserts runner-OBSERVED state == requested before any `[DONE]`), `/api/cron/exchange-jobs-reconcile` (45-min lost-callback backstop), kill switch `EXO_AUTOMATION_ENABLED`. keep_accessible DEFERS license removal in both paths (mailbox must be licensed at conversion; callback removes it post-verification, guarded by the runner's 50GB/holds/archive verdict); non-enabled tenants degrade to `[MANUAL]` + reason. Forwarding = phase 2. Enablement: `docs/runbooks/EXO_AUTOMATION_ENABLEMENT.md`.
- `2026-07-06` — **Sales Calculator integrated** as a staff-gated internal route `/admin/sales-calculator` (was a standalone Next 14 app on the Sales SharePoint). Copied VERBATIM — only import paths changed; config JSONs sha256-identical; quote math verified (25 engine parity checks vs hand-computed pricing.json values + 35-check UI drive incl. all exports). Theme scoped to a `.tct-calc-root` wrapper (never `<body>`); its Tailwind tokens resolve from `--tct-*` vars that exist only inside the wrapper. Deps: jspdf/jspdf-autotable/xlsx (client-side, lazy-imported). Side changes: tsconfig target es5→es2018 (type-check only; also fixed a pre-existing raw-tsc error), ESLint `no-explicit-any` off for the copied dirs only. **Pricing changes = edit `src/config/sales-calculator/*.json`, never `src/lib/sales-calculator/`.** Open pricing confirmations: `docs/reference/sales-calculator/OPEN_ITEMS.md`.
- `2026-07-06` — **Sales calculator: live pricing editor + reactive/proactive support model.** Money is now `pricing.json` defaults + DB overrides: raw-pg `sales_calc_pricing_overrides` (append-only audit rows; flat path→number map validated against pricing.json — can never add keys), edited at `/admin/sales-calculator/pricing` (staff view; saves need `system_settings`), applied by the calculator AND the authenticated e2e spec before quoting; graceful degradation to defaults with a visible banner. Service `include` became tri-state (`true` / `"billable"` = hourly T&M / `false`) — use `serviceInclusionState()`, never truthiness; Comprehensive & Ally mark Helpdesk + Remote Support `"billable"` (owner: Comprehensive = reactive, remediation approved + billed hourly; Complete = proactive, included), rendered as a Support Model row + ⏱ legend on the charts from `packages.json.supportModel`. **New table = POST `/api/migrations/run` once after deploy.** Full notes: `docs/gotchas.md` → Sales Calculator.
- `2026-07-06` — Staff can now **preview the agent training material** (`/agents/training`) without an agent login. New `resolveTrainingViewer()` (`src/lib/agent-training-access.ts`) is the single bridge between agent-portal auth and staff NextAuth: agent session wins; a logged-in staff user gets a read-only `TrainingChrome` "Admin Preview" bar (`src/components/agents/TrainingChrome.tsx`) instead of the agent nav/sign-out. Entry point: "View Training" button on `/admin/sales-agents`. Unauthenticated still redirects to `/agents/login`. Don't add a parallel auth bridge — extend this helper.
- `2026-07-08` — **CFO hiring calculator rebuilt as the New Hire Break-Even calculator** (`/admin/cfo/hiring`), a 1:1 port of the owner's breakeven workbook: 3 hire types (US W-2 / US 1099 / PH contractor) × 3 tiers, NY employer taxes with wage-base caps (FUTA/SUTA+RSF/SS), itemized tooling ($218.73/seat) + equipment + onboarding detail, billable-hour math, required billing rate / monthly revenue at target margin (+50/55/60% references), month-1 and year-1 totals. `src/lib/cfo/hiring.test.ts` locks parity with the workbook's computed cells. Saved assumptions are `version: 2`-guarded (`isHiringAssumptions`) — pre-v2 payloads fall back to defaults. Detail: `docs/gotchas.md` → CFO.
- `2026-07-09` — **Sales calculator: Ally "shared responsibility" display state, saved quotes, comparison exports.** (1) Ally (comanaged) renders Helpdesk / Remote Support / Vendor Management with a shared-with-your-IT icon via `packages.json → sharedServices` + `serviceDisplayState()` — DISPLAY ONLY; money treatment unchanged (`serviceInclusionState()` still drives calc; locked by `shared-display.test.ts`). (2) Quotes save/reload/edit: raw-pg **`sales_calc_saved_quotes`** (stores inputs only — loading reprices at current pricing + overrides; soft delete; `tableMissing` degrade) + `/api/admin/sales-calculator/quotes` CRUD + a Saved Quotes toolbar in the calculator. **New table = POST `/api/migrations/run` once after deploy.** (3) Quote Comparison exports landscape PDFs in internal (costs/margins) and customer versions; the internal button hides in customer view. Detail: `docs/gotchas.md` → Sales Calculator.
- `2026-07-10` — **Thread forms integration hardened to Thread's REAL Automation URL contract** (owner-verified docs: Thread POSTs bare JSON — no auth headers, no HMAC; expects `200 { success: 200, message }` with the link inside the message). The invented `x-thread-signature`/`THREAD_WEBHOOK_SECRET` design was replaced with a fail-closed URL key (`THREAD_AUTOMATION_KEY`, `checkAutomationKey()` in `api-auth.ts` — unset env = reject all). Company resolution is deterministic: `meta_data.company_id` (Autotask company ID) primary via shared `src/lib/form-links.ts`; fuzzy name match never runs after an exact-identifier miss; unresolved = 404 `not_configured`. Single-use links now real: `POST /api/forms/links/[token]/used` stamps `form_links.used_at` + `request_id` (token → hr_request → Autotask ticket = the chat-ticket merge chain; Thread chat-ticket id kept in `form_links.source_meta`). New columns are additive with writer-side backfill. Detail: `docs/gotchas.md` → Thread Integration.
- `2026-07-10` — **e2e gate made non-blocking on auto-merge** at the owner's explicit direction ("make it live now" + deploys taking too long): merge now requires secret-scan + quality only (~5-8 min); the Playwright suite still runs vs the preview as a post-hoc signal. Context: the owner initially attributed the day's delay to the gate — the actual delay was a review-hold (`[skip ci]`) he had requested plus a transient Vercel fetch error; the latency claim is still true in general (e2e gate = 15-45 min/push). The blocking gates that remain exist because the fully-ungated pipeline previously shipped broken commits to production.
- `2026-07-15` — **MCP connector gained HR Employee-Relations writes** to TCT's OWN HumanResources SharePoint site (`hr_er_log_append`, `hr_file_document` in `src/lib/mcp-hr-tools.ts` + `src/lib/hr/employee-relations.ts`). DIRECT writes (no staged gate — low-risk, human-approved text), audit-logged + read-back verified, behind kill switch `CONNECTOR_HR_WRITES_ENABLED`. Auth = a NEW dedicated least-privilege Entra app (`Sites.Selected` → `write` on ONLY the HR site), NOT the AZURE_AD staff-SSO app — chosen to keep an internet-reachable file-write credential out of the SSO/PTO/CFO secret. Non-obvious: Excel workbook API works app-only for org-owned SharePoint sites with `Sites.Selected` (docs' "Application: Not supported" is `/me`/user-shared only). Also closed the IT Glue TODO: `includeArchived` filter (default false) on the three doc-search tools. Full detail: `docs/gotchas.md` → "HR Employee-Relations records".
- `2026-07-15` — **MCP connector auth made provider-swappable to drop WorkOS.** `CONNECTOR_AUTH_PROVIDER` (`workos` default | `entra`) in `src/lib/connector/auth.ts` selects the OAuth authorization server; `route.ts` + `/.well-known/oauth-protected-resource` delegate to it. WorkOS path unchanged (default = zero prod impact); Entra path reuses the staff-SSO tenant and preserves per-user write attribution. Motivated by WorkOS desktop-connector sign-in never finalizing. Non-obvious: Entra v2 access-token `aud` is the app CLIENT ID (GUID), not the Application ID URI, so `CONNECTOR_ENTRA_AUDIENCE` = client id; email must be added as an Access-token optional claim. Cutover: `docs/runbooks/CONNECTOR_AUTH_ENTRA.md`.
- `2026-07-16` — **Both went LIVE.** Connector cut over to Entra in prod and the HR write tools are enabled; `hr_er_log_append` verified end-to-end against the real Employee Relations Log.xlsx. Two field lessons: (1) the connector endpoint had to MOVE to a new URL (`/api/connector/entra/mcp`) because Claude caches the OAuth authorization server per connector URL and kept replaying the old WorkOS token; (2) address Excel workbook tables by **NAME**, never Graph's braces-GUID `id` — `encodeURIComponent('{…}')` 404s every `/columns` `/rows` call (first-prod-call bug, fixed in `resolveLogTable`/`tableSeg`). Both in `docs/gotchas.md` + the runbook.
- `2026-07-16` — **MCP connector gained IT Glue document-folder tools** so connector-created SOPs stop defaulting to the org root: `itglue_list_document_folders` (resolve folder ids — never guess), `itglue_create_document_folder` (public API DOES support folder create; delete stays UI-only), `itglue_move_document` (reuses the existing `updateDocument` PATCH of `document_folder_id`, read-back verified — no parallel client). `itglue_create_document` now instructs folder-first placement. Folder endpoints are org-nested only; the folders index default (`omit` = ALL) is the opposite of the documents index (`omit` = root-only). Two parallel sessions shipped this same-day with identical tool names; implementations unified in the merge. Field notes: `docs/gotchas.md` → IT Glue.
- `2026-07-18` — **MCP connector gained read-only Datto RMM reporting tools** (17 `datto_rmm_*` tools in `src/lib/mcp-datto-rmm-tools.ts`) covering the API's full GET surface: account/rate status, sites, devices (per-site authoritative; account search by LIKE filters), class-aware hardware audits, software inventory, alerts (account/site/device × open/resolved), activity logs, job results, components/filters/users/variables. GET-only BY CONSTRUCTION via a new `DattoRmmClient.getV2()` passthrough (no method/body, `/api/v2/` only — the sole change to the shared client); unit test proxies the client and fails on any other method. Console links = the API's own `portalUrl`/`webRemoteUrl` (never a guessed pattern); alert/activity rows resolve links from cached sweeps. Proxy passwords redacted, masked variables forced `[MASKED]`. Write surface (quickjob/UDF/move/resolve/variables) deliberately deferred — would require the staged-write gate. Reference: `docs/reference/DATTO_RMM_CONNECTOR_TOOLS.md`; field notes: `docs/gotchas.md` → Datto RMM.
- `2026-07-18` — **Datto RMM pagination is 0-INDEXED** (live-confirmed: `page=1` empty with `totalCount: 213`; owner saw 0 alerts vs 13 in console). Fixed every sweep to start at `page=0` — including the ORIGINAL client loops, which had skipped the first 250 rows since the SOC session (the real cause of the "account devices returns 5 of hundreds" gotcha; device-sync cron + executive-summary open alerts were silently incomplete). Regression-locked: the tools' unit-test mock serves rows only on page 0. `docs/gotchas.md` → Datto RMM.
- `2026-07-19` — **Four-defect repair** (live-API-verified): DNSFilter query_logs takes DATE-ONLY from/to (full-ISO >9 days back = 400 — the cause of DNSFilter zeros) + blocked breakdown now paginates all pages; Datto EDR severity read from the list model's native `severity` (threatName/flagName/etc. don't exist on list rows); **SLA unified on the ticket_lifecycle engine** — dashboard/company/QBR/annual + ticket views all read `getLifecycleQualitySummary()` and the `completedDate <= dueDateTime` proxy is deleted (never revive it); reopens computed from status history with **null = not measured** (never a fabricated 0). Field notes: `docs/gotchas.md` → DNSFilter / Datto EDR / Reporting SLA & Reopen.
- `2026-07-20` — **Support metrics split HUMAN vs AUTOMATED via one shared classifier** (`src/lib/reporting/ticket-classification.ts` + `humanTicketSqlCondition()` for bulk SQL) after the monthly review counted SaaS Alerts/Datto EDR auto-tickets as support work (fabricated 0m response / 100% response SLA / 47-ticket backlog / blended resolution averages — live-confirmed on Tri-Bros June 2026: 47 created = 14 human + 33 automated). Source 8 "Monitoring Alert" = automated with BOTH-direction misfile rescues (34477 source-8-but-worked = human; 34369 default-source SOC alert = automated); every surface (business review, dashboard, company, annual, exec summary, TBR export, health score, aggregation, customer portal metrics) counts human only; automated tickets render as a new "Security & Monitoring Activity" section (no SLA/response math ever); lifecycle stores null SLA verdicts for automated tickets and quality reads classify-and-exclude. Also: status 52 "Complete - No Notify" + reopen status 22 added to the cold-start status defaults (52 missing = the fake backlog). Full detail: `docs/gotchas.md` → Reporting Human vs Automated.
- `2026-07-20` — **UniFi connector network create brought up to the 10.4.x Integration API.** On Network app 10.2+ a GATEWAY network create requires `ipv4Configuration` + `internetAccessEnabled` + `isolationEnabled` + `cellularBackupEnabled` (live 400 on TCR/10.4.57); `ipv4Configuration` (the subnet/DHCP object) wasn't even in the allowlist, so the connector could never set a VLAN's subnet/DHCP. Widened the `unifi_network` allowlist to the real API fields and added `normalizeUnifiChanges()` in `staged-writes-core.ts` — a pure pre-validation payload builder that expands convenience inputs (`subnet` CIDR + `dhcp*`) into a full `ipv4Configuration` and defaults the flags, so `name`+`vlanId` alone still creates a working VLAN while subnet/DHCP stay explicitly settable. Convenience keys are consumed pre-validation (never sent/stored); the staged-write approval flow, drift check, and single-target rule are unchanged. Also fixed `unifi_wlan` drift: added the `network` field (SSID→VLAN binding) and removed the phantom `uapsdEnabled`. firewall zone/policy + ACL create re-verified against the current published spec (v10.3.58; 10.4.x docs unpublished) — no drift. Full detail: `docs/gotchas.md` → UniFi + `docs/unifi-site-tools.md`.
- `2026-07-20` — **Connector staged-write drift check made key-order-insensitive** (follow-up after the UniFi network fix went live). Adding `ipv4Configuration` (the first nested object) to an allowlist exposed a latent bug: the staged `before` snapshot round-trips through a JSONB column, which reorders object keys, so `detectDrift`'s `JSON.stringify` compare false-drifted every gateway-network update/delete (`fields: ipv4Configuration`) though nothing changed. Fix: new pure `canonicalizeKeys()` in `staged-writes-core.ts` (deep key-sort, array order preserved) applied to BOTH sides before the UNCHANGED `detectDrift` in `unifi-staged-writes.ts`; real changes still caught. Detail: `docs/gotchas.md` → UniFi.
- `2026-07-21` — **Reporting first-response / first-touch definitions fixed** (follow-up to the human-vs-automated split, same Tri-Bros review): the regenerated PDF still showed 0m response / 100% response SLA / 0% FTR on the HUMAN tickets. Causes: (1) the first ticket note is the INTAKE record (tech logging the call they're on, or an integration posting at creation — `firstResponseDateTime == createDate` on 14/14 June tickets), read as a 0-minute response; (2) FTR used `≤1 note`, so an intake note + resolution note by the same tech failed it; (3) the resolution MEDIAN and per-priority resolution avg were computed over a different ticket population than their neighboring average/count. New shared helpers in `lifecycle.ts` — `resolveFirstResponse()` (staff-opened = "answered live at intake", null not 0; only genuine post-creation human activity by non-API accounts counts), `countDistinctHumanParticipants()` (FTR = single human owner), `getHumanResourceIds()` — used by business review, annual report, technician + ticket-list realtime queries; medians/priority avgs realigned to the same cohort as their counts; outlier-skewed months lead with the median in the customer narrative. Locked by `first-response.test.ts`. Full detail: `docs/gotchas.md` → Reporting First Response & First Touch.
- `2026-07-22` — **Offboarding license-removal ordering hardened against regression.** M365 license removal must run AFTER shared-mailbox conversion + delegate grants (an unlicensed mailbox can't be converted or delegated); this had regressed to implicit call-site order and recurred on Tri-Bros (originally Dan Brown Construction / Michael Beach). The earlier defer-license fix (2026-07-05) was still in place and NOT reverted — the gap was that nothing *enforced* the order. Made it explicit + guarded: new `OFFBOARDING_STEP_ORDER` constant + `assertLicenseRemovalAfterMailboxConversion()` in `src/lib/hr/offboarding-actions.ts`; `deriveRequestedOffboardingActions()` emits the plan in canonical order and self-asserts; `/api/hr/process` carries a runtime tripwire (fails the request if license removed inline for keep_accessible); locked by `offboarding-actions.test.ts`. All triggers (portal, Thread webhook, scheduled cron) converge on `/api/hr/process` — one engine, one fix. No change to which actions run. Full detail: `docs/gotchas.md` → HR Onboarding/Offboarding.
- `2026-07-21b` — **SLA reporting switched to Autotask's OWN per-contract result, Fully-Managed only** (owner-directed after the regenerated review still showed a wrong "Resolution SLA 50%"). We now sync Autotask's native SLA event datetimes onto `tickets` (`firstResponse*`/`resolutionPlan*`/`resolved*` DateTime + `slaHasBeenMet`; new columns → ALTER in `/api/migrations/run` + POST after deploy) and the lifecycle engine reads the verdict off them (`metByDue`, actual ≤ due) instead of recomputing against `reporting_targets` (deleted from `computeTicketLifecycleInMemory` — it drifted from each customer's contracted SLA). SLA is reported ONLY for the "TCT – Fully Managed IT Services" agreement (id 2; `getFullyManagedSlaId()`/`isFullyManagedSla()`/`isSlaReportableTicket()` in `sla-config.ts`, name-refreshed from the live SLA picklist during sync) AND human AND non-alert-queue — mirroring the ops SLA widgets. Business review omits the SLA section entirely for non-Fully-Managed customers (`servicePerformance.slaApplicable`). All 3 live SLAs: id 1 Standard / id 2 Fully Managed / id 3 No SLA (all automated tickets are on No SLA). Contract category↔SLA mismatches (e.g. AllSpec = Platinum but Standard SLA) are an Autotask CONFIG fix, not code — such customers correctly get no SLA section until fixed. Operator: POST migrations → re-sync tickets → recompute lifecycle. Locked by `sla-config.test.ts`. Full detail: `docs/gotchas.md` → Reporting SLA is Autotask's own.
- `2026-07-28` — **MCP connector gained per-call usage telemetry + an owner dashboard** at `/admin/connector/usage`. Capture is hooked ONCE, in the existing `recordingServer()` proxy in `capability-registry.ts` (it already saw every `registerTool`; it now wraps each handler too) — never per-tool logging, so tool #127 is measured for free; vendor/access/risk come from the existing `TOOL_FACTS`. New raw-pg table `connector_tool_calls` (**POST `/api/migrations/run` once after deploy**). **Arguments, response bodies and error messages are never persisted** — the column list is the enforcement and a unit test pins it; only names, classifications, outcomes, timings and response SIZE are stored. Refusals (staged-write gate / kill switch off / vendor not configured) are classified apart from real failures — unmet demand, not breakage — and failure buckets map from the shared `classifyError()`. Writes are itemised per technician as the security view. No Anthropic dollar figures (the connector makes zero Anthropic calls; bytes are a context-weight proxy, real spend stays at `/admin/monitoring`). Fire-and-forget and error-swallowing like `api-usage-tracker`. Full detail: `docs/gotchas.md` → MCP Connector Usage Telemetry.
- `2026-07-28` — **MCP connector gained read-only sales-pricing tools**: `sales_pricing_catalog` (complete model — every tier by its verbatim name incl. TCT Ally, per-unit cost AND sell rates by unit type, add-ons, M365 requirements/rates flagged billed-separately, Business Line floor/multiplier, rounding, and an explicit "no volume / term / industry / compliance modifiers exist") and `sales_pricing_quote` (the calculator as a tool: inputs → computed monthly + annual per tier), in `src/lib/mcp-sales-pricing-tools.ts`. Built because `/admin/sales-calculator*` is staff-auth'd AND `ROBOTS_DISALLOWED`, so pricing was pasted in by hand. Additive and READ-ONLY by construction — no write/stage/execute verb; rate edits still need a staff session with `system_settings` in the editor UI, which an MCP token cannot reach. New shared server-side reader `src/lib/sales-calculator/pricing-source.ts` (defaults + latest overrides row; the pricing GET route now uses it too) exposes `withEffectivePricing()`, because `applyPricingOverrides()` mutates the shared `pricingConfig` singleton — safe in a browser tab, a cross-request bug on a serverless instance; the helper resets → applies → runs a **synchronous** callback → resets, so the mutation window is atomic. Field finding: the published Wilmar + EZ Red comparison is NOT reproducible from "3 locations / 133 users / 132 PCs" — it also carried 6 servers and $350/mo of add-on lines; hand-derived totals come out short because of the Business Line charge, the internal-IT admin seat, and servers. Full detail: `docs/gotchas.md` → Sales Calculator.

- `2026-07-28` — **MCP connector failures now return a structured reason envelope**, and Autotask gained a live capability layer + Services/ServiceBundles write surface. `{failure:{reasonCode, message, evidence, remediation, fixableBy}}` on every Autotask tool failure, with a closed 7-code taxonomy defined **vendor-neutrally** in `src/lib/connector/failure-envelope.ts` (`NOT_IMPLEMENTED` / `UPSTREAM_UNSUPPORTED` / `POLICY_BLOCKED` / `PERMISSION_DENIED` / `PRECONDITION_FAILED` / `INVALID_INPUT` / `TRANSIENT`); `fixableBy` is derived from the code, never passed. **`UPSTREAM_UNSUPPORTED` for Autotask is ALWAYS derived from a live `entityInformation` lookup** (`autotask-capability.ts`, 30-min cache) — never hardcoded, because a stale "Kaseya can't do this" is the failure being eliminated; a cold-cache lookup failure returns `TRANSIENT`, and evidence is mandatory at the type level. New tools: `autotask_capability_check` (ask BEFORE attempting) and `autotask_surface_drift_report` (connector surface diffed against live metadata → tracked backlog). Fixed a real latent bug: `Services.markupRate` is `isReadOnly true` yet the old `service_pricing` allowlist accepted it. Services/ServiceBundles/bundle-membership create+update+delete added across the full writable field set — **still staged-approval only, no direct-write path** — with stage-time validation (billingCodeID useType 5, SLA picklist, duplicate warnings). `Services.canDelete` is FALSE (deactivate instead); `ServiceBundles.canDelete` is true. Full detail: `docs/gotchas.md` → "MCP Connector Failure-Reason Envelope" + "Autotask Services / Service Bundles". Other surfaces (IT Glue, UniFi, Datto RMM, HR) are NOT yet retrofitted and deliberately do not advertise an envelope.

- `2026-07-28` — **Sales calculator gained a delivery-labor / TRUE-margin layer.** The per-unit rates in `pricing.json` cover vendor tooling only, so the 85-93% margins the calculator reported were TOOLING margins, not profit. New `src/config/sales-calculator/labor.json` (cost-to-serve model) + pure `src/lib/sales-calculator/labor.ts` compose ON TOP of a finished `PackageQuote` — `calc.ts` is untouched, and `labor.test.ts` asserts quoted prices stay byte-identical (Wilmar comparison still exact). Model derived from 2,734 Autotask time entries (2026-02-01..07-27) joined to Datto RMM endpoint counts + Gusto loaded costs: **hours per endpoint per month scales with tier — Standard 0.239, Comprehensive 0.442, Complete 0.697** (Basic/Ally proxied, no live customers), blended delivery cost **$13.60/hr**, fixed pool **$15,520.79/mo**. `sales_pricing_quote` now returns `trueMargin` (contribution) + `capacity` per tier and labels `marginPct` as tooling-only; `sales_pricing_catalog` gained a `laborModel` section. **Quote CONTRIBUTION, never absorption** — fully-absorbed cost per endpoint falls as the base grows, so the same deal would price unprofitable today and profitable next quarter. The advisory retainer is deliberately in the fixed pool, not delivery capacity (owner-confirmed: not a billable resource; folding ~$4k/mo into ~4.7 logged hrs/mo would put the blended rate near $43/hr). labor.json holds AGGREGATE figures only — no individual pay rate, deliberately. Full detail: `docs/gotchas.md` → Sales Calculator.
- `2026-07-28b` — **Fixed the capability layer telling the owner a working write was impossible.** `autotask_capability_check` returned `UPSTREAM_UNSUPPORTED` + "cannot be written by anyone / do not offer to change this field" for `Services.periodType` — a field that is **required** on create, so a caller obeying it would have failed *every* Service create. The `isReadOnly` branch returned before consulting `createOnlyFields`, which the same function had already computed. Field verdicts are now **operation-aware** (`create` → permits true + `POLICY_GATED`; `update`/`delete` → immutable-after-create, explicitly *not* "unwritable"; unspecified → `permits: null` with the split in the evidence). Settled empirically at the same time: `periodType` IS settable on create and IS mandatory — Ally service ids 131–136 created with `periodType: 2` and read back Monthly, while the one create that omitted it died on Autotask's `500 Missing Required Field: periodType` **after** spending a human approval; it is now in `requiredOnCreate` for `service` + `service_bundle` so that fails at stage time instead. Also closed a drift blind spot: `createOnlyFields` skipped the `suspectAllowlistedFields` check entirely, so a typo in one was unreportable — only the read-only half of that check is inapplicable. Lesson: a false "the vendor can't" produced *by the layer built to eliminate false vendor claims* is the worst version of the bug; a read-only flag is a question, not a verdict. Full detail: `docs/gotchas.md` → "Autotask Services / Service Bundles".
- `2026-07-29` — **Same bug, third route — found by running the drift report the checklist asked for.** A caller can set a field by three routes (`changes`→`allowedFields`, create-only→`createOnlyFields`, **`parentId`→`parentIdField`/`parentIdFromField`**), and nothing consulted the third, so four fields the connector has always written were reported as unsupported on three surfaces at once: the drift report listed them as missing writable fields, `autotask_capability_check` said "no connector write area exposes it yet" (remediation: go build it), and `classifyRejectedFields` called them a connector gap. Worst case `ServiceBundleServices.serviceBundleID` — `isReadOnly true` AND required on every bundle-membership create — answered "cannot be written by anyone / do not offer to change this field", i.e. the `periodType` bug again on the one route the previous fix didn't enumerate. Settability is now decided in ONE place (`fieldSupplyRoutes()` / `settableFields()` in `staged-writes-core.ts`); read `allowedFields` directly and this recurs. Parent-id fields carry the same read-only exemption as create-only fields but keep "unknown to the API" scrutiny (a `parentIdField` typo breaks every create in the area, so it must stay reportable), and the stage tool description now names each area's `parentId`. Nothing in the *write path* changed — this was purely false reporting about it. Locked by 7 tests naming the exact wording that was wrong. Also from the same sweep: 0 suspect allowlisted fields across all 44 entities, so no allowlist is currently lying about a field. Full detail: `docs/gotchas.md` → "Autotask Services / Service Bundles" + "MCP Connector Failure-Reason Envelope".
- `2026-07-29b` — **Kaseya Quote Manager (Datto Commerce) wired into the MCP connector, read-only: 20 tools covering all 39 documented operations.** New client `src/lib/kaseya-quote-manager.ts` + tools `src/lib/mcp-kaseya-quote-manager-tools.ts`. **Read-only twice over**: `get()` is the only network path (no method/body parameter exists to override) AND the vendor API has no write surface — all 39 spec operations are GET, confirmed from the captured spec rather than trusted from Kaseya's statement. So there is no staged-write gate and no kill switch here. **The tool surface is GENERATED from one table (`KQM_RESOURCES`) and the coverage contract is a TEST, not a promise**: `mcp-kaseya-quote-manager-tools.test.ts` asserts every spec GET is reachable, every documented query param is exposed, **no undocumented param is advertised** (the API ignores unknown params instead of erroring, so a made-up filter would silently return everything while looking filtered), `modifiedAfter` appears on exactly the 15 supporting resources, and the spec still has zero writes. Three non-obvious facts: **paging is 1-INDEXED** (opposite of Datto RMM, whose page-0 bug silently dropped the first page of every sweep for months — regression-locked); **no response carries a total count** (bare arrays), so a capped sweep reports `truncated: true` and a single page reports `likelyMorePages` rather than inventing a total; and **the spec and Kaseya's help page contradict each other on auth** — spec says header `apiKey`, help page says query param. The header is the default (a generated spec beats hand-written prose) and `kqm_probe_connection` settles it live by trying each mechanism **in isolation**, never both at once, since a key sent two ways makes a success uninterpretable; `KASEYA_QUOTE_MANAGER_AUTH_MODE` applies the answer without a code change. `kqm_` adopted the failure envelope from its first commit (the taxonomy is vendor-neutral for exactly this). Rate limits are help-page-only (60/min, 20k/day) so they are enforced per-instance AND reactively via `withRetry` — the per-lambda window is a floor, not a guarantee. **No live call has been made**; nothing is confirmed about real API behaviour until the probe runs. Full detail: `docs/vendor-api/kaseya-quote-manager/COVERAGE.md`.
- `2026-07-29d` — **Kaseya Quote Manager verified live, and the probe's own reporting fixed.** First real calls: the key authenticates, `pageSizeCapHonoured: true`, `/v1/warehouse` returns the bare array the spec declares. The auth question is settled with an answer none of the three predicted branches covered — **the API accepts the key BOTH as a header and as a query parameter**, so neither vendor doc is wrong, each is incomplete. Header retained for a **security** reason rather than a doc-authority one: a query-parameter key lands in the URL and therefore in access logs, proxy logs and any error report echoing the request line. The probe had a real defect of the exact class this session kept fixing — it reported the FIRST successful mechanism and declared the other doc "can be disregarded", a confident claim its own observation never supported. It now returns `accepted` (every mechanism that worked) plus `working` (the mode the client will actually use, preferring the configured one over probe order), with a four-way recommendation; locked by one test per outcome including an assertion the wording never again says a doc can be disregarded. Lesson: **a diagnostic that collapses two observations into one answer is indistinguishable from one that guessed.** Full detail: `docs/gotchas.md` → Kaseya Quote Manager.

- `2026-07-29c` — **Three connector defects fixed, all reproduced live; two of them were a tool reporting success-shaped output on failure.** (1) **IT Glue cannot move a document between folders — ever.** `document_folder_id` is marked "Not permitted in PUT/PATCH, optional in POST" on `PATCH /documents/:id` AND the bulk PATCH; IT Glue drops the attribute and answers **200 unchanged**, which is why `itglue_move_document` returned `moved:false` with no error and the defect survived **twelve days** across two sessions (doc 24262329→5301326 on 07-17; doc 24227609→6255494 on 07-29). It now writes NOTHING and returns `UPSTREAM_UNSUPPORTED`/`fixableBy: vendor` citing the vendor's own wording, plus the document's current folder; `itglue_rename_document` **refuses before renaming** if `documentFolderId` is passed (it used to accept and ignore it) so nothing is half-applied; `updateDocument` types the field `never` and throws. Placement is create-time only — otherwise a human moves it in the UI. (2) **Autotask resource assignment was impossible**: any write setting `assignedResourceID` without `assignedResourceRoleID` gets `500 Data violation: …you must assign both…`, so assignment failed 100% of the time. Both tools now take `assignedResourceRoleID` and the pairing is enforced in the WRITER (`applyAssignedResourceRole()` in `autotask-write.ts`, inside `createTicket` AND `updateTicket`) so no caller can reintroduce a lone resource id; default **Engineer 29683355** (never Low/High Voltage 29683465); clearing an assignment does not acquire a role. Both VERIFY by read-back (`getTicketAssignment()`, a narrow query — not a widening of the shared `TICKET_QUERY_FIELDS`): an accepted PATCH that didn't stick is `PRECONDITION_FAILED`, not success. (3) **A 5xx is not proof of an outage.** `classifyError()` called every 500 a server error → `TRANSIENT`/`fixableBy: retry`/"wait briefly and retry", which sent the owner into a retry loop on a schema violation that could never succeed. New `data_violation` category matched on the BODY and checked before every status pattern (`isTransient: false`, so `withRetry` stops retrying these app-wide) → `PRECONDITION_FAILED` with the **vendor's own sentence** in `remediation` (`extractVendorRule()`); deliberately narrow — real 5xx outages stay `TRANSIENT`, a plain 400 stays `INVALID_INPUT`. Note `PRECONDITION_FAILED` derives `fixableBy: tct_human`; the "developer" routing lives in the remediation text because `FIXABLE_BY` is a total function of the code by design and re-pointing it would misroute staged-write drift. Every fix asserts the READ-BACK, not the HTTP status. No migration. Full detail: `docs/gotchas.md` → IT Glue / Autotask Integration / "MCP Connector Failure-Reason Envelope".

- `2026-07-30` — **Two connector defects fixed on ticket 34648, both the same shape: a tool asserting something it never checked.** (1) `autotask_add_customer_note` claimed to notify the ticket contact; it cannot. The REST `TicketNotes` entity has **12 fields and no notification field of any kind** — verified against the Kaseya docs AND this instance's live `entityInformation`, which agree exactly — because recipients live in the **UI-only Notification panel** and delivery otherwise depends on an Autotask **Event**, whose definitions have no REST surface. So nothing was invented: the description now states plainly that the tool does not and cannot notify, and the write instead **observes** what it can prove — the note read back (id, `publish` id **with its live label**, note type, attribution) plus `customerNotified`, derived from `Tickets.lastCustomerNotificationDateTime` before vs after the write and enriched from `NotificationHistory`. A false verdict is **fail-closed** ("the contact has NOT been emailed"). Two live findings: `publish` id **1 "All Autotask Users" is the CUSTOMER-VISIBLE value** (the Internal-cleared state per Kaseya's note-form docs) and there is no id 3 — the old code comment said `3=Customer-visible`; and the owner's hypothesis that status 19 "Customer Note Added" drives the customer email is **backwards** — that template is "Customer Note Added to Ticket - **Notify Assigned Resources**", fires when a *contact* posts, and emails TCT staff, so setting status 19 would have notified the wrong party. Any rule change is a manual admin task, not code. (2) Ticket reads permitted false absence claims about staff work: `autotask_ticket_notes` structurally excludes time entries, so Ghenel Bacalla's completed 2.67-hour build (time entry 13188) was invisible and reported as "no update showing the work finished" — while the ticket's own `lastActivityDate` (14:48:47) already contradicted the newest returned note (12:08:22), unflagged. New **`autotask_ticket_activity`** merges TicketNotes + TimeEntries + TicketAttachments into one timeline (source entity, author, timestamp, customer-visible vs internal), and all seven ticket reads now publish `lastActivityDate` + a computed **`activityGap`** with a warning naming the tool to call. Non-obvious: a timestamp gap check ALONE is insufficient (on 34648 the newest note is *newer* than the time entries, so the structural exclusion notice must fire unconditionally too); there is deliberately **no tolerance window** because a spurious "check the timeline" costs a tool call while a missed gap costs an employee a false accusation; `activityGap: null` means NOT MEASURED; and `TimeEntries` has **no `publish` field**, so time-entry visibility reports `unknown` with the reason rather than guessing. Locked by `autotask-activity.test.ts`, which replays the 34648 timestamps. No migration. Full detail: `docs/gotchas.md` → Autotask Integration + "Autotask ticket reads must never support a false absence claim".
- `2026-07-30b` — **`hr_er_log_append` was failing on EVERY call, and the root cause was the hardcoding, not the missing column.** `ER_COLUMNS` fixed the appended row at 14 values; the owner had added a 15th column to "Employee Relations Log.xlsx" — **"Meeting with Tech"** (free text for what the tech said when the issue was discussed, filled by hand on ER-0001/ER-0004) — so Graph rejected every append with `400 "The number of rows or columns in the input array doesn't match the size or dimensions of the range."` That workbook is a HUMAN'S document the owner edits, so any tool that fixes its shape in code guarantees the same outage on the next column. Fixed at the level of the defect: `readTableShape()` reads the table's header row on every call and pure `planErRow()` maps inputs onto the LIVE headers — width is `liveColumns.length` **by construction**, order follows the sheet (sorted by the API's own `index`, since the row is positional), unmatched live columns are padded `''` and reported in `unmappedColumns`, and header matching is case/punctuation-insensitive with aliases so a cosmetic header edit doesn't silently unmap a column. `ER_FIELDS` now declares only which headers we HAVE CONTENT FOR — explicitly not the row width. Also: `meetingWithTech` is now a real parameter; read-back verification reads the Entry ID at its live index rather than `[0][0]`; the HR surface was retrofitted to the **structured failure envelope** (the bare Graph 400 named no column and routed to nobody); and new read-only **`hr_er_log_columns`** reports the live shape WITHOUT writing — the diagnostic whose absence meant the only way to inspect the sheet was to attempt an append. Two non-obvious findings: **Excel tables carry no required-column metadata and Graph reports none**, so "required" can only be a header-text convention (trailing `*` / `(required)`, deliberately narrow — a bare `required` false-positives on "Required Action", and a spurious hard failure blocks logging a real HR issue); and **`ER-####` and `ER-DOC-####` have legitimately DIVERGED** (log at ER-0004, docs at ER-DOC-0002 — two entries logged with no document filed), so neither may ever be inferred from the other. Verified live: ER-0005 appended against the real 15-column workbook. No migration. Full detail: `docs/gotchas.md` → HR Employee-Relations records.
- `2026-07-31` — **The Employee Relations log gained an UPDATE path: `hr_er_log_update`.** Rows were stranded half-finished by design, not by accident — `hr_er_log_append`'s own `meetingWithTech` description tells the caller to leave it unset until the conversation with the technician has happened, which guarantees the field needs filling afterwards (same for `followUpStatus` on close and `linkedDocument` when a write-up is filed later; live case: ER-0005, appended 2026-07-31 with both blank). The new tool patches ONE existing row located by Entry ID and **can never append** — an unknown Entry ID is a `PRECONDITION_FAILED` listing the ids that DO exist, because a duplicate row would inflate the subject's disciplinary record, the one failure mode this log cannot tolerate; two rows with the same Entry ID is a refusal naming both row indices, never a guess. **Entry ID and Date Logged are unwritable BY CONSTRUCTION**: `ER_UPDATABLE_FIELDS = ER_FIELDS.filter(kind !== 'computed')` and the input schema is exactly `entryId` + those fields + the two append flags, so no parameter capable of carrying either value exists (Date Logged records when the entry was CREATED, not when it was edited). Three non-obvious decisions: (1) **single-cell PATCHes, never a row PATCH** — Graph's `rows/itemAt(index=N)` takes the whole row, which would replace any formula in it with a computed value and touch cells the caller never named, so the tool reads `/dataBodyRange?$select=address,values` for the absolute address and writes `/worksheets/{sheet}/range(address='<cell>')` once per changed column; (2) a **missing target column is a HARD failure here** though it is only a warning on append — append pads a column that lost nothing, whereas update was explicitly told to write that cell, so a partial patch is refused entirely; (3) **read-back verifies against the Entry ID, not a row position** (the Entry ID cell is re-read at its exact address before writing and the row re-located by id afterwards), and a `YYYY-MM-DD` expectation is matched against a numeric cell via the documented Excel serial epoch — a date-formatted column returns a SERIAL, and without that equivalence every date patch would report `verified: false` and teach the reader to ignore the flag. Append mode (`appendToSummary` / `appendToMeetingWithTech`, both default replace) joins AFTER sanitization and never re-sanitizes the existing cell, since `sanitizePlainText` strips newlines and would otherwise destroy the ones already there. **No live write test was run — there is no non-production copy of this workbook**; the live shape was confirmed read-only via `hr_er_log_columns` (Table1, 15 columns, 5 rows). No migration. Full detail: `docs/gotchas.md` → HR Employee-Relations records.
- `2026-07-29` — **Delivery Economics rebuilt on BILLED UNITS instead of endpoints** (owner: "we don't bill just on endpoints… our quotes are broken out"). New pure `src/lib/reporting/delivery-economics/billing-units.ts` reconstructs each customer's recurring invoice lines from Autotask — one line is split across THREE entities (`Services` = name, `ContractServices` = rate, `ContractServiceUnits` = quantity **one row per billing period, never sum it**) — and reports revenue, tooling cost, allocated labour and contribution **per user / per device / per server / per site / per company**, plus a tier unit-mix view and a contracted-vs-catalogue discount ledger. Read-only client additions `listContractServices()` / `listContractServiceUnits()`. Locked by `billing-units.test.ts`, which reproduces two real Complete Care invoices to the cent (EZ Red exactly $4,104.00; Tri-Bros $2,845.00 less $50.00 prorations). Why the old model was wrong: users and devices are independent counts whose ratio inverts between customers (15u/20d vs 32u/28d), so per-user services — a third to a half of managed revenue — had no cost basis. Non-obvious: rates must come from the CONTRACT (catalogue lists `TCT-Complete-User` at $80; live contracts bill $100 and $33.60); `Remote PC Access` matched `/\bpc\b/` and was miscounted as a managed device; and the labour fit gates on **adjusted** R² because at n≈10–25 raw R² scores size-independent noise at 0.48–0.62 — below threshold it falls back to a flat allocation and says `method: 'unit-share'`, which is a finding, not metadata. `labor.json` still costs per endpoint and now carries `_unitDenominatorLimitation`. Full detail: `docs/gotchas.md` → Delivery Economics report.
- `2026-07-28` — **Delivery Economics report** at `/admin/reporting/delivery-economics`: cost-to-serve, capacity and billing capture measured from Autotask time entries joined to Datto RMM endpoint counts. `src/lib/reporting/delivery-economics/` (pure analyzer + I/O service + raw-pg snapshots), `GET/POST /api/reports/delivery-economics`, weekly cron Mondays 09:00 UTC. **New table `delivery_economics_snapshots` = POST `/api/migrations/run` once after deploy.** Snapshots are **append-only** — the trend is the finding (internal-work share fell 66%→25% Feb→Jul 2026 while customer hours rose 40%); never upsert or prune. Two traps: Autotask TimeEntries 405s on a full-month window (fetch in half-month slices, de-dupe by id) and `companyID === 0` means internal, not null. Billing capture reads `billingStatus` from BillingItems, not the `billable` boolean. Non-billable time must be SPLIT before quoting recoverable revenue — alert-sized monitoring is correctly unbilled, only project-sized (2h+) write-offs are recoverable (conflating them overstated it ~10×). Full detail: `docs/gotchas.md` → Delivery Economics report.
- `2026-08-10` — **The MCP connector's ~75-minute disconnects are the DEFAULT ACCESS-TOKEN LIFETIME, and that lifetime is configurable — the fix needs nothing from Claude.** Entra mints access tokens at a random 60-90 min (75 avg); `AccessTokenLifetime` on a `tokenLifetimePolicy` raises that to a **24h maximum** (`23:59:59`), assignable to the one app. Three sessions had been trying to influence what the CLIENT requests because the Jan-2021 retirement of *refresh/session* lifetime config was misread as "token lifetimes aren't configurable" — access-token lifetime was never retired. Graph/PowerShell only (no portal UI), an **org-level policy silently overrides an app-level one** (check first), CAE's 24-28h extension can never apply here (needs client AND resource support), and the trade is a longer post-revocation window. Runbook: `docs/runbooks/CONNECTOR_AUTH_ENTRA.md` → "The fix that does not depend on Claude". **Also a same-day RETRACTION:** an earlier commit claimed `scp: "mcp.access"` proved `offline_access` was never granted and called the 07-28 fix disproven — it proves nothing, because `scp` carries only scopes *exposed by our own API* and reserved scopes (`offline_access`, `openid`, `profile`, `email`) are absent from it either way. **An absence guaranteed by the format carries no information**; the 07-28 fix is UNKNOWN, not disproven, and only `IsInteractive: False` rows in the Entra sign-in logs settle it. Same shape as `periodType` / `parentIdField` / the Kaseya probe, and it read as more rigorous than the measurement it displaced precisely because it was cheaper. Separately, a **hard** outage opened the same day (OAuth completes, Claude still shows *Connect*, zero tools) which the expiry alone does NOT explain, since a reconnect mints a fresh 60-90 minute token; server side was verified healthy first (metadata 200, correct 401 challenge, no 5xx, no connector deploy since 07-31 — the 08-06 middleware `/rtp` redirect excludes `api` by matcher). Leading candidate is an **expired app client secret**: the authorize leg still succeeds and shows "Connected" while the code→token exchange fails, so Claude replays the stale token. Owner check + recovery: `docs/runbooks/CONNECTOR_AUTH_ENTRA.md` → "Hard outage".

- `2026-08-10b` — **The MCP connector got its OWN OAuth authorization server** (`/api/connector/tct/mcp`), because the ~75-minute drops were never fixable from our side: claude.ai's custom-connector proxy **never refreshes OAuth tokens** ([claude-ai-mcp#228](https://github.com/anthropics/claude-ai-mcp/issues/228)) and specifically ignores the token endpoint of **external** IdPs (Entra/Okta/Auth0), while first-party connectors are unaffected — which is exactly why every other connector in the account stays up. With refresh unavailable, **the access-token lifetime IS the reconnect interval**, and Entra's ceiling is 24h. Ours defaults to **30 days**. Entra remains the identity provider (staff sign in with Azure AD at `/authorize`), so per-technician attribution is unchanged. Also kills the client secret — we support **DCR**, Entra does not, so the 08-10 lapsed-secret outage cannot recur. New: RFC 8414 AS metadata, RFC 9728 PRM (path-suffixed, since the bare one still describes the Entra mount), `/register` `/authorize` `/token`, `src/lib/connector/oauth/`. **New tables = POST `/api/migrations/run` once after deploy**; **new required env `CONNECTOR_OAUTH_SIGNING_KEY`** (fail-closed; rotating it is the break-glass revocation of every token). Tool surface extracted to `src/lib/connector/build-mcp-handler.ts` and shared by BOTH mounts — verified byte-identical to the previous inline body, so the Entra mount is behaviourally untouched and stays live as the rollback. Non-obvious: `offline_access` must be on the **authorization-server** document, not the protected-resource one (the 07-28 fix put it in the wrong file); the `scope` parameter on the **`WWW-Authenticate` 401** is Anthropic's documented first-choice lever and is now set explicitly rather than trusting metadata fallback; registration is deliberately NOT open (claude.ai + RFC 8252 loopback only); codes/refresh tokens stored SHA-256 only. Runbook: `docs/runbooks/CONNECTOR_AUTH_TCT.md`.

- `2026-08-20` — **Gift-basket campaign landing page at `/welcome`**, reached by the QR code on the drop-off flyer and nothing else. Self-contained by design: the visitor (a receptionist or office manager on a phone, ~30 seconds of attention, no prior relationship) books a meeting or sends an inquiry **without leaving the page** — inline Calendly, inline inquiry form, tap-to-call `tel:+16073417500` and tap-to-email `support@triplecitiestech.com`. Unlinked and noindex on the established `/rtp` + `/msa` pattern (robots metadata in `layout.tsx`; absent from `NAVIGATION` and `sitemap.ts`) — deliberately NOT via `robots.ts`, since a `Disallow` line there publishes the URL. Service names/descriptions render from `SERVICES` in `src/constants/services.ts`, so the page cannot drift from the real service copy and no new marketing claims were written. The real work was **extraction, not authoring**: the contact form became `src/components/shared/ContactForm.tsx` (now the only client-side caller of `POST /api/contact`, carrying all five `SPAM_PROTECTION.md` layers) and the Calendly widget became `src/components/shared/CalendlyEmbed.tsx`; `/contact` and `/schedule` consume both. Regression method worth reusing: the rendered body HTML of both existing pages was captured before and after, normalised, and diffed — **0 differences**, which caught two cosmetic drifts (a React `<!-- -->` text-node separator from `{label} *`, and a reordered Tailwind class) that a visual check would have missed. No new dependencies, no `next.config.js` change (CSP already allowed Calendly + Turnstile), no migration.

- `2026-08-25` — **MCP connector gained full Autotask PROJECT, TASK and CRM support: 22 tools** (`src/lib/mcp-project-tools.ts`) — create/update projects, phases and tasks; task and project notes (add + edit); task time entries (create + edit, ticket entries too); task secondary resources and predecessors; company and contact create/update. All DIRECT impersonated writes, not staged: the approval gate is for instance CONFIGURATION, these are single operational rows correctable in the UI. Every write is **read-back verified per field** and returns `PRECONDITION_FAILED` when a value did not stick. **The headline finding is a retraction**: "task PATCH returns 404 on all 3 entity paths" is deleted from `known-limits.ts`. `Tasks.canUpdate` is true; `ProjectTasks` **does not exist** as a REST entity here (its `entityInformation` 404s), so one of the three "paths" was never a path; and `updateTaskStatus()` caught every error and rethrew only the last, so the recorded symptom was never evidence about the other two. What the correct path actually returned is NOT established — the chain destroyed it. The row went because its evidence was unsound and the API contradicts its conclusion, not because a replacement explanation was confirmed. New `writeAtFirstWorkingPath()` fixes the class: **404 → next candidate, any other status → stop and surface it**, with every attempt reported as `pathAttempts`. Two live picklist bugs found the same way and fixed: `AT_TASK_STATUS_IN_PROGRESS` was **4, an id this instance does not have** (In Progress is **8**), and `AT_PROJECT_STATUS.ACTIVE` was **4 = "Change Order"** (In Progress is **2**) — both had been writing invalid/wrong statuses from the admin UI write-back path. Also non-obvious: a required + read-only parent id (`Projects.companyID`, `Phases.projectID`, `Contacts.companyID`) means **the URL supplies it and it is immutable**, not that nobody can set it — the `periodType` trap again; `Companies.canDelete` is false so company create refuses a duplicate name by default; `Contacts.isActive` is an integer; contact and time-entry DELETE are POLICY_GATED (Autotask permits both) not vendor-limited. One consequence worth knowing: `autotask_capability_check` derived "implemented" from the STAGED-write areas alone, so it told callers to *build* `Tasks.update` while the tool was live — a capability layer sending someone to rebuild what exists is the mirror of one claiming the vendor can't. Direct-write tools are now first-class in that verdict (`DIRECT_WRITE_TOOLS`, guarded by `autotask-drift.test.ts`). Full detail: `docs/gotchas.md` → "Autotask projects, tasks and CRM".

- `2026-08-25b` — **Five defects fixed from the Wilmar project-55 build (10 phases, 134 tasks through the connector).** (1) **Autotask returns HTTP 500 for request-shape rejections**, and the bare `'500'` test in `classifyError()` called them TRANSIENT with `fixableBy: retry` — advice that can never work. The 07-29 `data_violation` matcher missed them because it keys on the literal phrase "Data violation". `classifyError()` now inspects the BODY: a structured `errors[]` array means the vendor enumerated what is wrong with the request → `validation`/INVALID_INPUT, or `data_violation`/PRECONDITION_FAILED when the message is state-dependent. **Recognition is an ALLOWLIST** — the first version reclassified any `errors[]` body and broke two real retry tests using `{"errors":["internal error"]}`; an unrecognised body keeps retrying, because a needless retry costs a second and a suppressed one costs an outage recovery. (2) **Task assignment is a FOUR-field group** — `assignedResourceID` + `assignedResourceRoleID` + **`billingCodeID`** + **`departmentID`** — not the two-field Tickets pair, and **Autotask enforces resource-to-role pairings**: the old Engineer (29683355) default is held by only 4 of the team, so it failed for Kurtis, James, Jhomar and Benjamin. The role now defaults to the resource's OWN `isDefault` row, the pairing is validated BEFORE the write (`Tasks.canDelete` is false — a trial-and-error write is permanent), and `billingCodeID` is refused rather than guessed. New read `autotask_resource_roles` exposes the resource↔role join, which nothing did. (3) **FIVE wrong hardcoded picklist ids from one root cause**, all copied from Autotask's DEFAULT picklist: task status In Progress (4→8), project status ACTIVE (4→2, 4 is "Change Order"), **`AT_TASK_PRIORITY` LOW/HIGH were INVERTED** (live 1 is High, 3 is Low — silently swapping priority on every synced task), `autotask_add_project_note` defaulted `noteType` 3 which **does not exist** on ProjectNotes (8/5/12), and the shared note-`publish` help claimed "no id 3, use 4" — true of TaskNotes, **false of ProjectNotes**, which has 3 and lacks 4. Durable fix: `src/lib/connector/autotask-picklists.ts` resolves by LABEL at runtime and reports `resolvedFrom: 'fallback'` rather than falling back silently. **Never add a sixth constant, and never assume two entities share a picklist.** (4) **Autotask recalculates a project's `endDateTime` from its tasks** — 2026-10-01 became 2026-09-25 after 134 task writes. Read-back verification proves a field stuck at write time, NOT that it is still correct after later writes to related records; re-read the project after a bulk task build. Full detail: `docs/gotchas.md` → "Autotask task assignment, picklists and 500s".

- `2026-08-28` — **Connector gained `autotask_update_ticket`, and the IT Glue `archived` flag was unified onto one path.** (1) Ticket 35432 (T20260827.0018) landed from inbound email with `companyID 0` and could not be mapped; the connector could assign/status/resolve/note but never correct a ticket's CORE fields, so every mis-landed inbound ticket was a manual UI fix. The new tool takes `ticketId` plus at least one of nine fields — **all nine confirmed `isReadOnly false` against LIVE `entityInformation` BEFORE any code was written, so none were dropped** — PATCHes only what is supplied (no GET-merge), and is read-back verified per field with `PRECONDITION_FAILED` on a value that did not stick. Two visibility facts are in the description and re-emitted only when they fire: `companyID` **re-parents** the ticket (notification recipients, available contacts/contracts and portal visibility all follow the new company) and `contactID` **changes who Autotask emails**. Non-obvious: a new direct-write tool must be registered in **`DIRECT_WRITE_TOOLS` as well as `TOOL_FACTS`** — `autotask-drift.test.ts` caught the omission mid-build and named the cost, that `autotask_capability_check` would report `Tickets.update` as an unbuilt gap and send the next session to rebuild a live tool. (2) The IT Glue `archived` filter turned out to be **already built** (2026-07-15) across all three doc reads and all three code paths, but with **zero test coverage** and one real inconsistency: `itglue_org_documents` carried the flag only at `attributes.archived` while the search tools returned it top-level, so a caller that learned `doc.archived` from a search read `undefined` — falsy, i.e. a stale SOP reading as current. `withArchivedFlag()` adds the top-level key **additively** (attributes untouched) and 16 tests now pin the contract, including that `includeArchived` is passed THROUGH to the SQL index rather than applied after it, since a flag dropped there leaks archived docs for indexed orgs only. Lesson worth keeping: **check whether the feature already exists before building it** — half this task was already shipped and only its tests were missing. Full detail: `docs/gotchas.md` → "Autotask ticket field updates" + IT Glue.

- `2026-08-28b` — **The re-parent `autotask_update_ticket` was built for could never run, and its failure told the caller to retry.** (1) Autotask stamps `companyLocationID` on EVERY ticket at create time and refuses the whole PATCH while it or a stale `contactID` still belongs to the previous company — both reproduced live on test ticket 35437, both `isReadOnly false` / `isRequired false`, so a connector gap and not a vendor limit. A company change now moves both in the SAME call: the location is RESOLVED to the new company's own `isPrimary`+`isActive` row (a fact read from `CompanyLocations`, and what Autotask's own ticket form defaults to), and a stale contact is CLEARED (a contact is a person, so no non-arbitrary replacement exists, and one left attached keeps emailing the previous customer about a ticket that is no longer theirs). **`contactID` is the majority case** — a mis-filed inbound ticket almost always has one, and the first version of the fix handled only the location. **CONTRACTS BLOCK THE MOVE AND CANNOT BE CLEARED HERE** — settled only on the third attempt, by an A/B with one variable (same ticket, source, destination and contract, differing only in whether `contractID: null` was sent): WITH the null Autotask returns 200, moves the company and does NOT clear the contract; WITHOUT it, 500 `contractID [...] is not associated to companyID [...]`. So the null is not a workaround but a CORRUPTION — it suppresses the check and persists the old value, leaving a ticket on one company holding another's contract. The tool never sends one and REFUSES the move up front with `PRECONDITION_FAILED` naming the blocking contract; the way through is a `contractID` belonging to the new company in the same call, or clearing it in the UI first. Vendor asymmetry worth knowing: **a null `contactID` clears the field, a null `contractID` is silently discarded.** Three rules came out of getting this wrong twice: a claim is licensed by the operation it was TESTED on and nothing else (the first claim generalised from an adjacent operation); when a result surprises you, enumerate EVERY difference (the retraction read one difference off a run that had two, the second being a null this session's own fix had added); and a verification exemption covers OBSERVING state, never CLAIMING an action (`clearedOnReparent` was built from what the PATCH SENT, so it announced a clear in the same note that reported the field unchanged — success-shaped output on failure, reintroduced inside the fix written to prevent it). Anything reported as done is now filtered against the read-back. (2) That failure classified `TRANSIENT` / `fixableBy: retry`, which the taxonomy forbids for a deterministic rejection. The cause was not a missing phrase: recognition of a structured `errors[]` body WAS a list of thirteen sentences someone had already met. The rule is now general — **a rejection refers to the REQUEST** (names a field, quotes an id it carried, states a rule it broke, or names the state blocking it), **a fault does not** — so it covers the whole Autotask surface without the failing string being added anywhere. Generic faults wrapped in an errors array still retry, and a truncated body no longer reads its own JSON bracket as an id. Third time a lookup table has been the defect rather than its contents (`periodType`, `parentIdField`, this): when a check is a list of previously-observed cases, the next unobserved case is the guaranteed next bug. Full detail: `docs/gotchas.md` → "Autotask ticket field updates" + "An `errors[]` body is recognised by SHAPE".

## Critical Gotchas — digest

**The full list in `docs/gotchas.md` is mandatory reading.** These are the catastrophic-if-missed rules:

1. **Prod migrations**: a schema field without a matching `ALTER TABLE` in `/api/migrations/run` = production crash ("column does not exist"). Prisma migration files alone do nothing.
2. **Raw-pg subsystems**: HR, M365, reporting, SOC, CFO settings — never convert to Prisma; never remove SSL config or `globalThis` pool caching.
3. **Every external `fetch()` needs `signal: AbortSignal.timeout(…)`** (15s auth, 30s data) — a hung API blocks the whole serverless function.
4. **Cron jobs never return 500 for transient failures** — `classifyError()` + 200 `{ transient: true }`. Cron auth is the Vercel-set `Authorization: Bearer <CRON_SECRET>` header.
5. **API catch blocks never return 200 with empty data** — return 4xx/5xx with `{ error }` or the UI lies to the user.
6. **SOC enrichment must be scoped to the customer** — platform mappings first, name-match fallback, SKIP rather than query MSP-wide. Unscoped queries have leaked other customers' data before.
7. **Auth helpers are mandatory**: `checkSecretAuth()` for secret routes, `checkCsrf()` on customer mutations, `checkRateLimit()` on auth endpoints. Never inline secret checks, never log auth details, never hardcode fallback signing keys.
8. **React**: `useState(prop)` needs a `useEffect` sync; every useEffect fetch needs AbortController cleanup; no `overflow-hidden` around absolute dropdowns; every admin subsection needs `loading.tsx`; error boundaries wrap major sections.
9. **Quality gates before every push**: build + lint + e2e. A fix isn't confirmed until the previously failing test passes (`npm run debug:failures`, `/admin/debug/failures`).
10. **Use `NEXT_PUBLIC_BASE_URL`** for generated URLs — never hardcode the domain. Strict CSP: third-party resources require `next.config.js` header updates.
11. **Autotask**: data is authoritative; **an HTTP 500 with a structured `errors[]` body is a REQUEST rejection, not an outage — never retry it** (recognised by SHAPE — does the message refer to the request? — never by a phrase list); **changing a ticket's `companyID` is rejected outright while its `companyLocationID`, `contactID` or `contractID` still belongs to the old company — location and contact move in the same PATCH, but a contract CANNOT be cleared by the API (a null is accepted, silently discarded, and suppresses the check into a corrupt cross-company state), so that move is refused up front**; statuses/priorities are instance-specific picklists (`autotask_entity_picklist`) — **NO task status id 4 (In Progress is 8), project status 4 is "Change Order", and task priority 1 is HIGH not Low**; **task assignment needs four fields together (resource + role + billingCodeID + departmentID) and the role must be one that resource actually holds** (`autotask_resource_roles`); never silently catch phase/task API errors. **The "task PATCH 404s on all 3 paths" claim is RETRACTED (2026-08-25)** — `Tasks.canUpdate` is true, one of those three paths (`ProjectTasks`) is not an entity on this instance at all, and the fallback chain that produced the claim rethrew only its last error, so it was never evidence. What the correct path returns is resolved live by `autotask_update_task` and reported as `pathUsed`.
12. **Don't guess about external APIs** — read the vendor docs first; every integration here has at least one non-obvious quirk (see field notes in `docs/gotchas.md`).

## E · Memory Map (`docs/`)

- `architecture.md` · `system-map.md` · `data-model.md` · `session-summary.md` · `current-tasks.md` — bootstrap docs 1–5
- `gotchas.md` — bootstrap doc 6: full gotchas + Autotask/M365/SOC/SaaS Alerts/portal/CFO field notes, temporary dev shortcuts, pre-launch cleanup checklist
- `coding-standards.md` · `qa-standards.md` · `UI_STANDARDS.md` — implementation and QA rules
- `runbooks/` — `RUNBOOK.md` (incident response), `DEBUGGING_WORKFLOW.md` (e2e self-healing debug loop), `CREDENTIALS_MIGRATION.md` (secret rotation)
- `plans/` — design documents (`SOC_REDESIGN_PLAN.md`, `OLUJO_PROJECT.md`, `PROJECT_OVERVIEW.md`, …)
- `reference/` — setup + feature guides (`AUTOTASK_SYNC.md`, `REPORTING_ARCHITECTURE.md`, `ONBOARDING_PORTAL.md`, `CUSTOMER_INVITE_AND_ONBOARDING.md`, `AZURE_AD_SETUP.md`, `BLOG_SYSTEM_README.md`, `CLAUDE_SESSION_PREFERENCES.md`, …)
- `archive/` — superseded docs and old session summaries
- Cross-cutting handoffs: `SOC_CROSSSTACK_HANDOFF.md`, `CFO_HANDOFF.md`, `COMPLIANCE_PLAYBOOK.md`

**Session reset**: before ending a session, update `session-summary.md` + `current-tasks.md`, commit, push. New sessions reload context from the bootstrap docs.

## F · References

- **Production**: https://www.triplecitiestech.com · **Preview**: `https://<branch-name>-triplecitiestech.vercel.app`
- **Hosting**: Vercel (auto-deploy all branches; `main` → production; region `iad1`)
- **Env vars**: full list in `.env.example`. Required core: `DATABASE_URL`, `PRISMA_DATABASE_URL`, `NEXTAUTH_SECRET`, `NEXTAUTH_URL`, `AZURE_AD_*` (3), `ANTHROPIC_API_KEY`, `RESEND_API_KEY`, `TURNSTILE_SECRET_KEY`, `NEXT_PUBLIC_TURNSTILE_SITE_KEY`, `NEXT_PUBLIC_BASE_URL`. Autotask: `AUTOTASK_API_*` (4) + `MIGRATION_SECRET` (auths `/api/migrations/*`, `/api/test-failures/migrate`, `/api/soc/migrate`, `/api/autotask/trigger`). `CRON_SECRET` — all `/api/cron/*` (Vercel sets the Bearer header). M365 multi-tenant app: `M365_PORTAL_CLIENT_ID/SECRET` (TCT-published app reg, NOT per-customer). Integrations: `ROCKETCYBER_API_TOKEN`, `SOC_INGEST_SECRET`, `SAAS_ALERTS_API_KEY` + `SAAS_ALERTS_REFRESH_TOKEN`, `SAAS_ALERTS_WEBHOOK_TOKEN`, `ENCRYPTION_MASTER_KEY_V1`, `E2E_TEST_SECRET`.
- **Secrets are NEVER stored in this file or any doc.** They live in Vercel env vars only (`vercel env pull`). If a session needs one for a one-off command, ask the operator to paste it into that message — never commit it.
- Always give **full URLs** (e.g. `https://www.triplecitiestech.com/api/soc/migrate`), never partial paths.

## G · Project-specific overrides & user preferences

- **User runs Windows (PowerShell)** — always give commands in PowerShell syntax (`Invoke-RestMethod`/`Invoke-WebRequest`), never bash/curl/Mac/Linux.
- **Mobile + desktop both matter** — every UI change is verified at `sm`/`md` and `lg`+ breakpoints; flag mobile-affecting changes explicitly.
- **Forbidden colors** (yellow/amber/gold/brown/orange) — see Code Conventions; this is an owner mandate, not a suggestion.
- **Temporary development shortcuts exist intentionally** (auto-merge to main, preview auto-deploys, query-param secret fallback, impersonation + debug endpoints). Preserve them, don't expand them, don't copy them as patterns. Full list + pre-launch hardening checklist: `docs/gotchas.md`.

## Self-Improvement Protocol

When the user corrects a mistake or a session reveals a new convention:
1. Fix the immediate issue.
2. Record the lesson in `docs/gotchas.md` under the right section; promote a one-liner to the Critical Gotchas digest above only if missing it would break production.
3. Bump this file's *Last updated* date and commit the doc update alongside the fix.

The same mistake must never happen twice across sessions.
